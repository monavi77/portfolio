
  # Portfolio Template Design

  This is a code bundle for Portfolio Template Design. The original project is available at https://www.figma.com/design/bSOMS8GDdWkm1m3Stfz392/Portfolio-Template-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  